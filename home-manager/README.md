# Nix Dotfiles

## `app`
Application default configurations.

## `system`
Covers default systems/integration-level things (e.g. shell config, browser
defaults).

## `machine`
Per-machine configs. Symlink `home.nix` to one of these.
